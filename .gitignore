# Locally installed agent skills (npx skills add).
# The skills themselves are not versioned — .claude/hooks/session-start.sh
# reinstalls them on each web session, so these stay out of the repo.
.agents/
.claude/skills/
skills-lock.json

# Personal Claude Code settings; .claude/settings.json is shared and tracked.
.claude/settings.local.json
